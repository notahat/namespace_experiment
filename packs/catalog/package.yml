enforce_dependencies: true

# The root package provides ApplicationRecord, which every model here inherits
# from.
dependencies:
  - "."

metadata:
  # Everything under app/ is placed in the Catalog namespace by
  # automatic_namespaces, so app/models/book.rb defines Catalog::Book.
  automatic_pack_namespace: true
